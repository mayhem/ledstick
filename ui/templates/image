{% extends 'layout' %}
{% block body %}
<div>
    <div id="images">
       {% for url in urls %}
           <div class="image">
               <img class="image" src="{{ url }}"></img>
           </div>
       {% endfor %}
    </div>
    {% if next_page > 0 %}
        <div id="pagination">
            <a href="/images/{{ next_page }}" class="next">next</a>
        </div>
    {% endif %}
</div>
{% endblock %}
